export enum InputTypeEnum {
  EMAIL = 'email',
  TEXT = 'text',
  DECIMAL = 'decimal',
  NUMERIC = 'numeric',
  TEL = 'tel',
  URL = 'url',
  SEARCH = 'search',
  // HIDDEN = 'hidden',
  // PASSWORD = 'password',
}

export enum InputKeyboardTypeEnum {
  DEFAULT = 'default',
  NUMBER = 'number-pad',
  DECIMAL = 'decimal-pad',
  NUMERIC = 'numeric',
  EMAIL = 'email-address',
  URL = 'url',
  PHONE = 'phone-pad',
  // HIDDEN = 'hidden',
  // PASSWORD = 'password',
}
