import { UiCheckIcon } from '.';

import type { Meta } from '@storybook/react';

const Story: Meta<typeof UiCheckIcon> = {
  component: UiCheckIcon,
  title: 'Template',
};
export default Story;

export const Primary = {
  args: {},
};
