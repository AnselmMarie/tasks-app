import { UiIcon } from '.';

import type { Meta } from '@storybook/react';

const Story: Meta<typeof UiIcon> = {
  component: UiIcon,
  title: 'Icon',
};
export default Story;

export const Primary = {
  args: {},
};
