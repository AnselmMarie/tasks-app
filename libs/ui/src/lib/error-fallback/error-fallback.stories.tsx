import { UiErrorFallback } from '.';

import type { Meta } from '@storybook/react';

const Story: Meta<typeof UiErrorFallback> = {
  component: UiErrorFallback,
  title: 'Ui/WIP/Error Fallback',
};
export default Story;

export const Primary = {
  args: {},
};
